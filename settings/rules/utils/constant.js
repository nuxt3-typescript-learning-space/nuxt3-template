/**
 * リアクティブ関数名リスト
 * @type {('ref' | 'toRefs' | 'storeToRefs' | 'computed')[]}
 */
export const REACTIVE_FUNCTIONS = ['ref', 'toRefs', 'storeToRefs', 'computed'];

/**
 * 引数の`.value`チェックから除外する関数名リスト
 * @type {string[]}
 */
export const EXCLUDED_FUNCTIONS = [];
